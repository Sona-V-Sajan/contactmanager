import 'package:flutter/material.dart';

class Edit_Contact extends StatelessWidget {
  const Edit_Contact({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("data"),
    );
  }
}
